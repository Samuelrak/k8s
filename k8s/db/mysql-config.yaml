apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: travel
data:
  MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: "true"
  MARIADB_USER: "root"
  HOSTNAME: "mysql-service.travel.svc.cluster.local"
